# Copy to .env.local for local overrides (never commit .env.local)

# Leave empty in development — Vite proxy handles /api/* → localhost:8000
# Set to your backend URL in production, e.g.:
#   VITE_API_BASE_URL=https://songpractice-api.onrender.com
VITE_API_BASE_URL=
